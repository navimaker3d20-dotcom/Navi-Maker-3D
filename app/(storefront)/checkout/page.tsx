import { CheckoutFlow } from "@/features/checkout/CheckoutFlow";

export const metadata = { title: "Checkout — Navi Maker 3D" };

export default function CheckoutPage() {
  return <CheckoutFlow />;
}
